{
  "version": 2,
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "./",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/$1" }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}